# yaml-language-server: $schema=https://raw.githubusercontent.com/Azure/azure-dev/main/schemas/v1.0/azure.yaml.json

# name: rfp-analyzer
# metadata:
#     template: azd-init@1.10.3
# services:
#     src:
#         project: src
#         host: containerapp
#         language: python
#         docker:
#             path: Dockerfile

name: multiagent-poc    # <-- Change your project name here!

services:
    rfp-analyzer:        # <-- Change your service name here!
        project: src
        host: containerapp
        language: python
        docker:
            path: Dockerfile

